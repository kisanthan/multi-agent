# --- Secrets ----------------------------------------------------------------
# .env holds local provider credentials. .env.example is the tracked template.
.env
.env.*
!.env.example
.streamlit/secrets.toml

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# --- Virtual environments ---------------------------------------------------
.venv/
venv/
env/
ENV/

# --- Test, coverage, type and lint caches -----------------------------------
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/
.dmypy.json
.runtime_logs/
reports/

# --- Editors and tooling ----------------------------------------------------
.claude/
.agents/
.idea/
.vscode/
*.swp
*~

# --- Runtime artifacts ------------------------------------------------------
# Writable copies used by the app. The immutable, versioned originals live
# under data/demo/ and are deliberately not ignored.
data/masterdata.db
data/masterdata.db-wal
data/masterdata.db-shm
data/manifest.json
data/inbox/
data/checkpoints.sqlite
data/checkpoints.sqlite-wal
data/checkpoints.sqlite-shm
data/*.db-journal
data/*.sqlite-journal

# Legacy generated names from before the English path migration.
data/stammdaten.db*
data/eingang/

# --- OS ---------------------------------------------------------------------
.DS_Store
Thumbs.db

# Local authentication, backup and release state (never distribute)
.runtime_secrets/
data/backups/
release/

data/layout-deployment.json
